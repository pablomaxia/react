import "./App.css";
import Formulario from "./hooks/Formulario";

const App = (props) => {
  return (
    <div className="App">
      <header className="App-header">
        <Formulario />
      </header>
    </div>
  );
};

export default App;
